{
  "buildCommand": "npx turbo run build --filter=@simis/web",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://mainsite-api.vercel.app/api/:path*"
    }
  ]
}
